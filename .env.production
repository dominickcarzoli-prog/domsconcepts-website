# Etsy-backed public catalogue (approved + visible D1 listings).
# Also set in Cloudflare Pages → Settings → Environment variables → Production:
#   VITE_USE_ETSY_CATALOGUE = true
VITE_USE_ETSY_CATALOGUE=true
